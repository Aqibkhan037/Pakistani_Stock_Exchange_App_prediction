import 'package:flutter/material.dart';

class InformationTap extends StatelessWidget {
  const InformationTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
